# Valid Minimal Fixture

This fixture intentionally keeps a complete baseline for policy checks.
It is longer than one hundred bytes so R-01 length constraints pass.

## Install
Run the setup command from project root.

## Usage
Run policy-check against this repository clone.

## Version
0.0.0
