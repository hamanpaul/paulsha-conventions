name: policy-check

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    uses: ./.github/workflows/reusable-policy-check.yml
    with:
      policy_profile: stage-driven
      policy_version: "1.0.0"
